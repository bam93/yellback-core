# Crowd pack — bundled default sound pack.
#
# Clips here are PLACEHOLDERS, generated by Scripts/generate_placeholder_clips.swift
# (mono Float32 .caf at 44.1 kHz). Each clip is a short, distinguishable
# noise designed to verify SoundEngine playback and tier selection. They
# are NOT audience reactions — Session 12 sources real CC0/CC-BY clips.
#
# Schema: see SoundPack.swift / PackLoader.swift.

id: crowd
name: Crowd

tiers:
  low:
    - clap_short_a.caf
    - clap_short_b.caf
  medium:
    - cheer_mid_a.caf
    - cheer_mid_b.caf
  high:
    - roar_long_a.caf
    - roar_long_b.caf
