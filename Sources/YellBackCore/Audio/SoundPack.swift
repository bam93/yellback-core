import Foundation

/// An on-disk sound pack: a directory containing `pack.yaml` and a set of
/// `.caf` clips grouped by intensity tier.
///
/// The bundled Crowd pack ships inside the YellBackCore resource bundle. Extra
/// packs live in the consumer's configured `packs_directory`.
struct SoundPack {}
